import React from 'react';
// import loader from '../assets/loader.svg'

const Loader = () => {
    return (
        <div className='loader-wrapper'>
            {/* <img src={loader} alt='loader' width={30} height={30} /> */}
            ...Loading
        </div>
    )
}

export default React.memo(Loader);